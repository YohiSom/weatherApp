import React, { useEffect } from "react";
import { useAppContext } from "../context/appContext";

function Test() {
  //   const { newCity, searchLocation } = useAppContext();

  //   const newName = async () => await newCity(searchLocation);

  //   useEffect(() => {
  //     searchLocation && newName();
  //   }, [searchLocation]);
  return <div>Test</div>;
}

export default Test;
